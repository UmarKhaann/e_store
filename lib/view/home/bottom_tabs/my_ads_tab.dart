import 'package:flutter/material.dart';

class MyAdsTap extends StatelessWidget {
  const MyAdsTap({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Posts'),
      ),
      body: const Column(
        children: [],
      ),
    );
  }
}
